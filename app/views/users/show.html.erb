<div class="container">
  <div class="row">
    <div class="col-md-offset-3 col-md-6 col-md-offset-3">
      <h1>Thông tin tài khoản</h1>
      <h3>Tên: <%= current_user.name %></h3>
      <h3>Ngày sinh: <%= current_user.day_of_birth %></h3>
      <h3>Giới tính: <%= current_user.gender %></h3>
      <h3>Số điện thoại: <%= current_user.phone_number %></h3>
      <h3>Email: <%= current_user.email %></h3>
      <h3>Tài khoản được tạo vào ngày: <%= current_user.created_at %></h3>
      <h3>Lần đăng nhập cuối cùng vào ngày: <%= current_user.last_sign_in_at %></h3>
    </div>
  </div>
</div>
